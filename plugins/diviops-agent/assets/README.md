# Plugin runtime assets

This directory contains installed plugin UI assets. WordPress.org directory banners, icons and screenshots are maintained separately in `docs/wordpress-org-assets/` in the development repository and published to the top-level SVN `/assets` directory. See that directory's README for current capture provenance and release rules. Do not copy directory screenshots into this runtime folder.

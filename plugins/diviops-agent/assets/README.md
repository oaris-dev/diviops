# WordPress.org Asset Plan

This directory currently contains plugin UI brand assets used by the installed plugin.

For a future WordPress.org SVN submission, prepare these files in the top-level
WordPress.org SVN `/assets` directory, not in this installed plugin runtime
`assets/` directory:

- `banner-1544x500.png` and `banner-772x250.png`: DiviOps product banner showing the WordPress REST bridge and MCP server pairing.
- `icon-256x256.png` and `icon-128x128.png`: DiviOps mark on a clear background, readable at plugin-directory sizes.
- `screenshot-1.png`: DiviOps Agent admin screen or plugin activation state.
- `screenshot-2.png`: Example AI client MCP configuration with all secrets redacted.
- `screenshot-3.png`: Example successful capability/connection check with no private site data.

Do not include Application Passwords, license keys, customer site URLs, cookies, or other credentials in screenshots. Keep screenshots focused on the Free plugin and avoid implying that Pro-only MCP tools are included in the Free WordPress.org plugin.
